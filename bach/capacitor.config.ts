import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sentamil.bachflowers',
  appName: 'Malar App',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
